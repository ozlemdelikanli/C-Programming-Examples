#include<stdio.h>
#include<string.h>
void main()
{
	register int i;
	char name[100];
	printf("enter a string:");
	gets(name);
	
	printf("the reverse of string:");
	for(i=strlen(name)-1; i>=0; i--)
		printf("%c", name[i]);
}
